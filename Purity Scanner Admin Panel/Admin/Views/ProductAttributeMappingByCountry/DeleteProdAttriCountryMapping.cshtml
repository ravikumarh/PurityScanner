@model Admin.Models.clsProductAttributeMappingByCountry


@using (Html.BeginForm("ProdAttriCountryMappingDelete", "ProductAttributeMappingByCountry", FormMethod.Post))
{
     
   <div class="form-horizontal">
    
        <div class="form-group">
            <div class="col-md-12">
                <h3>Are you sure you want to delete this?</h3>
                @Html.HiddenFor(Model => Model.ProductAttributeCountryMappingID)
                @Html.HiddenFor(Model => Model.FilterAttributeID)
                @Html.HiddenFor(Model => Model.FilterProductID)
                @Html.HiddenFor(Model => Model.FilterCountryCode)
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-8 col-md-10">
                <input type="submit" value="Delete" class="btn btn-danger" />

                <button type="button" data-dismiss="modal" class="btn btn-default">Cancel</button>
            </div>
        </div>
    </div>
}

